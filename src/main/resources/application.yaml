spring:
  datasource:
    username: sa
    password: admin
    url: jdbc:h2:mem:library
  h2:
    console:
      enabled: true
      path: /h2
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: none
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
